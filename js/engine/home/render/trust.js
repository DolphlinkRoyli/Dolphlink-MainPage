/**
 * (Retired) Carrier Network & Compliance renderer.
 *
 * This file is kept as a compatibility stub so old service-worker caches
 * that pre-date the deletion don't 404 on the import line in cached
 * `js/engine/home/index.js`. Once `dolphlink-v297+` ships and clients
 * have rotated, this file can be deleted from disk and dropped from the
 * service worker PRECACHE list.
 */
export function renderTrustWall() { /* no-op */ }
