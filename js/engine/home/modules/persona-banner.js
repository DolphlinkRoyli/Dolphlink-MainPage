/**
 * DEAD FILE — superseded by `dept-explorer.js`.
 *
 * The earlier separate persona-banner (4 rotating quotes above the
 * Departments grid) was merged into the unified `.dept-explorer`
 * (tabs + active panel) component.  No HTML, JS or CSS references
 * this file any more, and it is NOT in the SW PRECACHE list.
 *
 * Safe to delete on next repo cleanup.  Stub kept so older deployed
 * copies of `home/index.js` that still import bindPersonaBanner don't
 * 404 — the function is a no-op.
 */
export function bindPersonaBanner() {
  /* no-op — see dept-explorer.js */
}
