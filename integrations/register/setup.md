# DEPRECATED — early-version document

This file described the original "mobile-only floating Connect button" design
that has since been completely replaced by the in-page Request-Briefing modal
(see `js/register.js` + `css/register.css`).

Safe to delete this file. The Apps Script backend doc that is still relevant
lives at `integrations/register/AppsScript.gs`.
