# DEPRECATED — `/cards/` mini-app retired

The standalone `/cards/` page was retired and replaced by:

- **Public-facing landing page** at `/c/?u=<localpart>` — source: `templates/card-page.html` + `css/card.css` + `js/card.js`. Deploy artifact: `c/index.html` (regenerated by `python3 build/build.py`)
- **In-modal team card view** inside the Request-Briefing modal (see `js/register.js`, `css/register.css`)

This entire `integrations/cards/` folder is safe to delete via Explorer.
Nothing on the live site references it any more.
