/**
 * DEPRECATED — superseded by the simplified /c/ landing page.
 * The Drive photo-proxy this script provided is no longer used; member.cardImage
 * URLs in content/cards.json point directly at Drive share links instead.
 * Safe to delete this file (and the whole integrations/cards/ folder).
 */
